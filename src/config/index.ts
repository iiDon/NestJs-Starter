import { ClsConfig } from './cls';
import { DotEnvConfig } from './dotenv';
import { RateLimiterConfig } from './rateLimiter';

export const config = {
  cls: ClsConfig,
  dotEnv: DotEnvConfig,
  rateLimiter: RateLimiterConfig,
};

export default config;
